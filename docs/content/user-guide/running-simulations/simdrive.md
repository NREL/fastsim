# What is a SimDrive Object?
## Combining a Vehicle and Drive Cycle

In FASTSim, a `SimDrive` object is a combination of a `Vehicle` and a `Cycle`. In concept, it is combining *what a vehicle is* and *what the vehicle does* to create a simulation scenario.

For information on how to edit simulation parameters, see [](editing-sim-params)